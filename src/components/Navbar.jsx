function Navbar () {

    return (
        <nav className="navbar container">
            <a href="#">[Home]</a>
            <div className="login">
            <a href="#">[Crear cuenta]</a>
            <a href="#">[Login]</a>
            </div>
        </nav>
    )

};

export default Navbar;